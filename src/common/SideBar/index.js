import { StyledSideBar, StyledRectangle, StyledShape } from "./styled";

const SideBar = () => {
  return (
    <StyledSideBar>
      <div>
        <StyledRectangle />
        <StyledShape />
      </div>
    </StyledSideBar>
  );
};

export default SideBar;
